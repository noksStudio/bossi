-- ═══════════════════════════════════════════════════════════════════════════
-- 0021 · פולואפ עם שעה, לא רק תאריך
--
-- `next_follow_up_at` (0020) היה `date` — מספיק ל"מתי", לא ל"מתי בדיוק".
-- הופך ל-`timestamptz`, כמו כל תאריך אחר במערכת (CLAUDE.md, "תמיד").
--
-- `using next_follow_up_at::timestamptz` ולא ברירת מחדל חדשה — הערכים
-- הקיימים (תאריך בלי שעה) הופכים לחצות באזור הזמן של השרת, וזה בסדר:
-- מי שקבע רק תאריך לא ציפה לשעה מדויקת, והוא עדיין יופיע כ"להיום".
-- ═══════════════════════════════════════════════════════════════════════════

alter table platform_prospects
  alter column next_follow_up_at type timestamptz using next_follow_up_at::timestamptz;
